# --- AI-assistant context (for Claude, not for users of this repo) ---
CLAUDE.md
CLAUDE.local.md
.claude/
AGENTS.md
CONTEXT.md
*.plan.md
docs/ai-context/
memory/

# Brainstorming companion scratch (superpowers) — local only.
.superpowers/

# --- Secrets ---
.env
.env.*
!.env.example
*.pem
client_secret*.json

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Offline question-generation staging output (ADR 0024) — reviewed by hand into
# the real bank, never committed as-is.
*.staging.yaml

# --- Node / frontend ---
node_modules/
dist/
*.local

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Terraform (infra/, ADR 0029) ---
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# infra/.terraform.lock.hcl is intentionally NOT ignored — it is committed.
